services:
  backend:
    build: ./backend
    restart: unless-stopped
    expose:
      - "8000"

  web:
    build: ./frontend
    restart: unless-stopped
    depends_on:
      - backend
    ports:
      - "${PORT:-8081}:80"
