FROM python:3.12-slim

ENV PYTHONUNBUFFERED=1 \
    PYTHONDONTWRITEBYTECODE=1 \
    PIP_NO_CACHE_DIR=1

WORKDIR /app

# Install uv (fast Python package manager)
RUN pip install --no-cache-dir uv==0.5.4

COPY pyproject.toml ./
RUN uv pip install --system --no-cache fastapi[standard] uvicorn[standard] httpx pydantic \
    python-multipart pillow imageio-ffmpeg

COPY app ./app

EXPOSE 8000
CMD ["uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "8000"]
